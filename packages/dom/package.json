{
  "name": "@routersaurus/dom",
  "version": "0.4.10",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "private": false,
  "files": [
    "dist/**/*"
  ],
  "dependencies": {
    "typescript": "5.2.2"
  },
  "devDependencies": {
    "@routersaurus/types": "*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "example": "turbo run publish:package --scope @routersaurus/dom -- --otp 064252",
    "publish:package": "yarn publish --new-version `node -e \"console.log(require('../../package.json').version);\"` --non-interactive "
  }
}
